import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { api } from '../api/client';

type WizardData = { source: 'UNIVERSITY' | 'ORGANIZATION' | 'INDEPENDENT'; programmeId: string; specialtyId: string; preferredCountryId: string; preferredCityId: string; preferredStartDate: string; preferredEndDate: string; clinicalInterests: string; preferredInstitutionId: string; noPreferredInstitution: boolean };
const empty: WizardData = { source: 'INDEPENDENT', programmeId: '', specialtyId: '', preferredCountryId: '', preferredCityId: '', preferredStartDate: '', preferredEndDate: '', clinicalInterests: '', preferredInstitutionId: '', noPreferredInstitution: false };
export default function ApplicationWizard() { const [step,setStep]=useState(1); const [data,setData]=useState(empty); const [message,setMessage]=useState(''); const navigate=useNavigate(); const update=(key: keyof WizardData, value: string|boolean)=>setData({...data,[key]:value}); async function submit(event:FormEvent){event.preventDefault();try{const response=await api.post('/applications', {...data, programmeId:data.programmeId||null, specialtyId:data.specialtyId||null, preferredCountryId:data.preferredCountryId||null, preferredCityId:data.preferredCityId||null, preferredInstitutionId:data.preferredInstitutionId||null, preferredStartDate:data.preferredStartDate||null, preferredEndDate:data.preferredEndDate||null}); await api.post(`/applications/${response.data.data.id}/submit`); navigate('/student/dashboard');}catch(error:any){setMessage(error?.response?.data?.message ?? 'Application could not be submitted.');}} return <main className="content-page wizard-page"><div className="eyebrow">New application</div><h1>Build your clinical attachment request.</h1><div className="stepper">{['Applicant','Academic','Preferences','Documents','Review','Submit'].map((label,index)=><span className={step===index+1?'active':''} key={label}>{String(index+1).padStart(2,'0')} {label}</span>)}</div><form className="form-card wizard-card" onSubmit={submit}>{step===1&&<><h2>Applicant information</h2><p>Your independent pathway remains available without university or organization affiliation.</p><label>Applicant type<select value={data.source} onChange={e=>update('source',e.target.value)}><option value="INDEPENDENT">Independent applicant</option><option value="UNIVERSITY">University student</option><option value="ORGANIZATION">Organization / institution student</option></select></label></>}{step===2&&<><h2>Academic information</h2><label>Programme<input value={data.programmeId} onChange={e=>update('programmeId',e.target.value)} placeholder="Programme ID or name"/></label><label>Specialty<input value={data.specialtyId} onChange={e=>update('specialtyId',e.target.value)} placeholder="Specialty ID or name"/></label></>}{step===3&&<><h2>Clinical attachment preferences</h2><label>Preferred country<input value={data.preferredCountryId} onChange={e=>update('preferredCountryId',e.target.value)} placeholder="Country ID or name"/></label><label>Preferred city<input value={data.preferredCityId} onChange={e=>update('preferredCityId',e.target.value)} placeholder="City ID or name"/></label><div className="form-row"><label>Start date<input type="date" value={data.preferredStartDate} onChange={e=>update('preferredStartDate',e.target.value)}/></label><label>End date<input type="date" value={data.preferredEndDate} onChange={e=>update('preferredEndDate',e.target.value)}/></label></div><label>Clinical interests<textarea value={data.clinicalInterests} onChange={e=>update('clinicalInterests',e.target.value)} rows={4}/></label><label className="check"><input type="checkbox" checked={data.noPreferredInstitution} onChange={e=>update('noPreferredInstitution',e.target.checked)}/> I do not have a preferred institution</label>{!data.noPreferredInstitution&&<label>Preferred institution<input value={data.preferredInstitutionId} onChange={e=>update('preferredInstitutionId',e.target.value)}/></label>}</>}{step===4&&<><h2>Documents</h2><p>Document upload foundation is next. Required document types will be configurable by programme.</p><div className="upload-placeholder">Passport, student ID, university letter, transcript, CV, and other documents</div></>}{step===5&&<><h2>Review</h2><p>Applicant type: <strong>{data.source}</strong></p><p>Clinical interests: {data.clinicalInterests||'Not provided'}</p><p>Institution: {data.noPreferredInstitution?'Assign through AZAAM':data.preferredInstitutionId||'Not provided'}</p></>}{step===6&&<><h2>Submit application</h2><p>Review complete. Submit this application for AZAAM review.</p>{message&&<p className="form-message">{message}</p>}</>}<div className="wizard-actions">{step>1&&<button type="button" className="button secondary" onClick={()=>setStep(step-1)}>Back</button>}{step<6&&<button type="button" className="button" onClick={()=>setStep(step+1)}>Continue</button>}{step===6&&<button className="button">Submit application</button>}</div></form></main> }
