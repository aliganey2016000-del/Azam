import { FormEvent, ReactNode, useEffect, useState } from 'react';
import { Navigate, NavLink, Route, Routes, useNavigate } from 'react-router-dom';
import { api } from './api/client';
import ApplicationWizard from './pages/ApplicationWizard';
const rolePaths: Record<string, string> = { SUPER_ADMIN: '/admin/dashboard', AZAAM_STAFF: '/admin/dashboard', UNIVERSITY_USER: '/university/dashboard', ORGANIZATION_USER: '/organization/dashboard', SUPERVISOR: '/supervisor/dashboard', STUDENT: '/student/dashboard' };
function Header() { return <header className="site-header"><NavLink className="brand" to="/">AZAAM <span>International Medics Network</span></NavLink><nav><NavLink to="/about">About</NavLink><NavLink to="/clinical-attachments">Clinical Attachments</NavLink><NavLink to="/how-it-works">How It Works</NavLink><NavLink to="/contact">Contact</NavLink></nav><div className="header-actions"><NavLink className="text-link" to="/login">Login</NavLink><NavLink className="button" to="/apply">Apply now</NavLink></div></header>; }
function PublicPage({ title, body }: { title: string; body: string }) { return <><Header /><main className="content-page"><div className="eyebrow">AZAAM International Medics Network</div><h1>{title}</h1><p>{body}</p><NavLink className="button" to="/apply">Start an application</NavLink></main></>; }
function Home() { return <><Header /><main><section className="hero"><div className="hero-copy"><div className="eyebrow">Clinical attachment platform</div><h1>Connecting medical students with quality clinical training opportunities.</h1><p>AZAAM facilitates clinical attachments, supervised medical training, and institutional collaboration.</p><div className="hero-actions"><NavLink className="button" to="/apply">Apply for clinical attachment</NavLink><NavLink className="button secondary" to="/contact">Partner with AZAAM</NavLink></div></div><div className="hero-panel"><span className="number">01</span><strong>Discover</strong><p>Find a pathway into supervised clinical learning.</p><span className="number">02</span><strong>Apply</strong><p>Submit your profile and attachment preferences.</p><span className="number">03</span><strong>Grow</strong><p>Track your journey with the right institutions.</p></div></section><section className="bands"><article><span>For students</span><h2>A clearer application journey.</h2><p>Build your profile, select preferences, upload documents, and follow your application status.</p><NavLink to="/for-students">Explore student pathway →</NavLink></article><article><span>For institutions</span><h2>Structured collaboration.</h2><p>Connect universities and host organizations with a focused clinical training workflow.</p><NavLink to="/for-organizations">Explore institution pathway →</NavLink></article></section><section className="trust-strip"><div><strong>TO BE CONFIRMED</strong><span>Approved programme information</span></div><div><strong>TO BE CONFIRMED</strong><span>Partner institution information</span></div><div><strong>TO BE CONFIRMED</strong><span>Official contact details</span></div></section></main></>; }
function Apply() { const options = [['UNIVERSITY','University Student','Applying through a university pathway'],['ORGANIZATION','Organization / Institution Student','Applying through an organization pathway'],['INDEPENDENT','Independent Applicant','Applying without university or organization affiliation']]; return <><Header /><main className="content-page"><div className="eyebrow">Application entry point</div><h1>What type of applicant are you?</h1><p>Choose the pathway that best describes your application. Independent applicants do not need an affiliation.</p><div className="choice-grid">{options.map(([value,title,description])=><NavLink className="choice" key={value} to={`/register?source=${value}`}><span className="choice-index">{value === 'INDEPENDENT' ? '03' : value === 'UNIVERSITY' ? '01' : '02'}</span><h2>{title}</h2><p>{description}</p><span>Continue →</span></NavLink>)}</div></main></>; }
function Register() { const query = new URLSearchParams(location.search); const source = query.get('source') ?? 'INDEPENDENT'; const [form,setForm] = useState({ fullName:'', email:'', phone:'', password:'', nationality:'' }); const [message,setMessage] = useState(''); const navigate=useNavigate(); async function submit(event:FormEvent){event.preventDefault();try{await api.post('/auth/register',{...form,accountType:'STUDENT',source});const {data}=await api.post('/auth/login',{email:form.email,password:form.password});sessionStorage.setItem('azam_token',data.data.token);navigate('/student/dashboard');}catch(error:any){setMessage(error?.response?.data?.message ?? 'Registration could not be completed.');}} return <><Header /><main className="content-page narrow"><div className="eyebrow">{source} applicant</div><h1>Create your account.</h1><p>Your initial account is short and focused. You can complete the full profile after registration.</p><form onSubmit={submit} className="form-card"><label>Full name<input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Nationality<input value={form.nationality} onChange={e=>setForm({...form,nationality:e.target.value})}/></label><label>Password<input required minLength={12} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{message&&<p className="form-message">{message}</p>}<button className="button">Create student account</button></form></main></>; }
function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const navigate=useNavigate();async function submit(event:FormEvent){event.preventDefault();try{const {data}=await api.post('/auth/login',{email,password});sessionStorage.setItem('azam_token',data.data.token);navigate(rolePaths[data.data.user.roles[0]]??'/');}catch{setError('Unable to sign in with those credentials.');}}return <><Header/><main className="content-page narrow"><div className="eyebrow">Secure access</div><h1>Welcome back.</h1><form onSubmit={submit} className="form-card"><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-message">{error}</p>}<button className="button">Sign in</button><NavLink to="/register?source=INDEPENDENT">Create a student account →</NavLink></form></main></>}
function Dashboard({role}:{role:string}){const [applications,setApplications]=useState<any[]>([]);useEffect(()=>{if(role==='STUDENT')api.get('/applications').then(({data})=>setApplications(data.data.items)).catch(()=>undefined)},[role]);return <><nav className="app-nav"><NavLink className="brand" to="/">AZAAM <span>/ {role.replace('_',' ')}</span></NavLink><button className="button secondary" onClick={()=>{sessionStorage.removeItem('azam_token');location.href='/login'}}>Sign out</button></nav><main className="dashboard"><div className="eyebrow">Operational workspace</div><h1>{role==='STUDENT'?'Your attachment journey':'Your AZAAM dashboard'}</h1><p className="lead">Your secure workspace for clinical training coordination.</p><div className="stat-grid"><div className="stat"><span>Account</span><strong>Active</strong></div><div className="stat"><span>{role==='STUDENT'?'Applications':'Workspace access'}</span><strong>{role==='STUDENT'?applications.length:'Scoped'}</strong></div><div className="stat"><span>Next step</span><strong>{role==='STUDENT'&&applications.length===0?'Apply now':'Review'}</strong></div></div>{role==='STUDENT'&&applications.length===0&&<div className="empty-state"><h2>Start your clinical attachment application</h2><p>Tell us about your pathway, academic background, and preferred clinical experience.</p><NavLink className="button" to="/student/applications/new">Apply now</NavLink></div>}</main></>}
function Protected({role,children}:{role:string;children?:ReactNode}){return sessionStorage.getItem('azam_token')?(children??<Dashboard role={role}/>):<Navigate to="/login" replace/>}
export default function App(){return <Routes><Route path="/" element={<Home/>}/><Route path="/about" element={<PublicPage title="About AZAAM" body="AZAAM's organizational details and approved public information are TO BE CONFIRMED."/>}/><Route path="/clinical-attachments" element={<PublicPage title="Clinical attachments" body="Explore clinical attachment opportunities and supervised training pathways. Programme details are TO BE CONFIRMED."/>}/><Route path="/for-students" element={<PublicPage title="For students" body="A focused pathway for students to create a profile, apply, and track their clinical attachment journey."/>}/><Route path="/for-universities" element={<PublicPage title="For universities" body="University collaboration details, requirements, and approved relationships are TO BE CONFIRMED."/>}/><Route path="/for-organizations" element={<PublicPage title="For host organizations" body="Host organization requirements and verification information are TO BE CONFIRMED."/>}/><Route path="/how-it-works" element={<PublicPage title="How it works" body="Discover, apply, verify, approve, place, supervise, track, evaluate, and certify."/>}/><Route path="/contact" element={<PublicPage title="Contact AZAAM" body="Official contact information is TO BE CONFIRMED."/>}/><Route path="/apply" element={<Apply/>}/><Route path="/register" element={<Register/>}/><Route path="/login" element={<Login/>}/><Route path="/admin/dashboard" element={<Protected role="ADMIN"/>}/><Route path="/student/dashboard" element={<Protected role="STUDENT"/>}/><Route path="/student/applications/new" element={<Protected role="STUDENT"><ApplicationWizard/></Protected>}/><Route path="/university/dashboard" element={<Protected role="UNIVERSITY_USER"/>}/><Route path="/organization/dashboard" element={<Protected role="ORGANIZATION_USER"/>}/><Route path="/supervisor/dashboard" element={<Protected role="SUPERVISOR"/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
