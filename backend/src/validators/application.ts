import { z } from 'zod';
export const profileSchema = z.object({ fullName: z.string().min(2).max(160), phone: z.string().max(40).optional(), nationality: z.string().max(100).optional(), universityId: z.string().uuid().nullable().optional(), organizationId: z.string().uuid().nullable().optional(), programmeId: z.string().uuid().nullable().optional(), countryId: z.string().uuid().nullable().optional() });
export const applicationCreateSchema = z.object({ source: z.enum(['UNIVERSITY','ORGANIZATION','INDEPENDENT']), universityId: z.string().uuid().nullable().optional(), programmeId: z.string().uuid().nullable().optional(), specialtyId: z.string().uuid().nullable().optional(), preferredCountryId: z.string().uuid().nullable().optional(), preferredCityId: z.string().uuid().nullable().optional(), preferredStartDate: z.coerce.date().nullable().optional(), preferredEndDate: z.coerce.date().nullable().optional(), clinicalInterests: z.string().max(4000).optional(), preferredInstitutionId: z.string().uuid().nullable().optional(), noPreferredInstitution: z.boolean().default(false) });
