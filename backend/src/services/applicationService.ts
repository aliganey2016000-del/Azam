import { ApplicationStatus, StudentSource } from '@prisma/client';
import { prisma } from '../utils/prisma';
import { ConflictError, ForbiddenError, NotFoundError } from '../utils/errors';
const transitions: Record<ApplicationStatus, ApplicationStatus[]> = { DRAFT: ['SUBMITTED'], SUBMITTED: ['UNDER_REVIEW'], UNDER_REVIEW: ['DOCUMENTS_REQUIRED','APPROVED','REJECTED'], DOCUMENTS_REQUIRED: ['SUBMITTED','REJECTED'], APPROVED: ['PLACEMENT_PENDING'], PLACEMENT_PENDING: [], PLACED: [], SUPERVISOR_ASSIGNED: [], ACTIVE: [], COMPLETED: [], CERTIFICATE_ISSUED: [], REJECTED: [] };
export async function listApplications(userId: string, roles: string[]) { const where = roles.includes('SUPER_ADMIN') || roles.includes('AZAAM_STAFF') ? {} : { student: { userId } }; return prisma.application.findMany({ where, include: { student: true, programme: true, specialty: true, university: true }, orderBy: { createdAt: 'desc' } }); }
export async function createApplication(userId: string, input: any) { const student = await prisma.student.findUnique({ where: { userId } }); if (!student) throw new NotFoundError('Student profile not found'); if (input.source === 'INDEPENDENT' && (input.universityId || input.organizationId)) throw new ConflictError('Independent applicants cannot have an affiliation'); const count = await prisma.application.count(); const applicationNumber = `AZM-${new Date().getFullYear()}-${String(count + 1).padStart(6, '0')}`; return prisma.application.create({ data: { ...input, applicationNumber, studentId: student.id, source: input.source as StudentSource }, include: { student: true } }); }
export async function getApplication(userId: string, roles: string[], id: string) { const app = await prisma.application.findUnique({ where: { id }, include: { student: true, documents: { include: { document: true } }, history: true, programme: true, specialty: true, university: true } }); if (!app) throw new NotFoundError(); if (!roles.some((role) => ['SUPER_ADMIN','AZAAM_STAFF'].includes(role)) && app.student.userId !== userId) throw new ForbiddenError(); return app; }
export async function transitionApplication(actorId: string, roles: string[], id: string, target: ApplicationStatus, comment?: string) { const app = await getApplication(actorId, roles, id); if (!roles.some((role) => ['SUPER_ADMIN','AZAAM_STAFF'].includes(role)) && target !== 'SUBMITTED') throw new ForbiddenError(); if (!transitions[app.status].includes(target)) throw new ConflictError(`Cannot change application from ${app.status} to ${target}`); return prisma.$transaction(async (tx) => { const updated = await tx.application.update({ where: { id }, data: { status: target, rejectedReason: target === 'REJECTED' ? comment : undefined, rejectedAt: target === 'REJECTED' ? new Date() : undefined } }); await tx.applicationStatusHistory.create({ data: { applicationId: id, fromStatus: app.status, toStatus: target, changedById: actorId, comment } }); await tx.auditLog.create({ data: { userId: actorId, action: `APPLICATION_${target}`, entity: 'Application', entityId: id, newValue: { status: target } } }); await tx.notification.create({ data: { recipientId: app.student.userId, title: `Application ${target.toLowerCase()}`, message: comment ?? `Your application status is now ${target}.`, type: 'APPLICATION' } }); return updated; }); }
